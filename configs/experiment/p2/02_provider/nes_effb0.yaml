# @package _global_
# timm efficientnet_b0. Provider held to timm; compare against the matching architecture
# under 01_architecture (torchvision).
defaults:
  - /experiment/p2/00_baseline/nes_baseline
  - _self_

experiment:
  name: p2_provider_timm_effb0

model:
  image_input:
    backbone:
      architecture:
        source: timm
        name: efficientnet_b0
        output_dim: auto
        input_channels: 3
      weights:
        source: library
