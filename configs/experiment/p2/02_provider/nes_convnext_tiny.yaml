# @package _global_
# timm convnext_tiny. Provider held to timm; compare against the matching architecture
# under 01_architecture (torchvision).
defaults:
  - /experiment/p2/00_baseline/nes_baseline
  - _self_

experiment:
  name: p2_provider_timm_convnext_tiny

model:
  image_input:
    backbone:
      architecture:
        source: timm
        name: convnext_tiny
        output_dim: auto
        input_channels: 3
      weights:
        source: library
