# @package _global_
defaults:
  - /experiment/p2/07b_transfer-denticle-type/_base
  - _self_

experiment:
  name: p2_transfer_type_unfrozen_mlp

model:
  embedding_adapter:
    enabled: true
    type: mlp
    output_dim: 512
    hidden_dims: [512]
    activation: gelu
    dropout: 0.1

training:
  freeze:
    backbone:
      policy: none
