# @package _global_
# torchvision efficientnet_b0. Provider held to torchvision; compare against the same
# architecture under 02_provider (timm) to isolate the provider effect.
defaults:
  - /experiment/p2/00_baseline/nes_baseline
  - _self_

experiment:
  name: p2_arch_tv_effb0

model:
  image_input:
    backbone:
      architecture:
        name: efficientnet_b0
