# Auto-generated by tests/fixtures/build_fixtures.py -- do not edit by hand.
# Example dojo data config for this committed Tier-1 fixture. The fixture carries
# a canonical `split` column (train/val), so it is consumed via split_column
# rather than split_from_filename.
backend: parquet_images
manifest_uri: ./tests/fixtures/plankton-toyset/data.parquet
split_column: split
sample_id_column: ifcb_roi_pid
images:
  column: image
  bytes_field: bytes
  path_field: path
source_extra_columns:
  - classname
  - original_label
targets:
  species:
    label_index_column: label
    label_name_column: classname
    type: multiclass_classification
    missing_policy: error
